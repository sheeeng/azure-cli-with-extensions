name: Run Pre-Commit via Nix

permissions:
  actions: write
  contents: read

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

concurrency:
  group: "${{ github.workflow }}-${{ github.head_ref || github.sha }}"
  cancel-in-progress: true

jobs:
  run-pre-commit:
    name: Run Pre-Commit
    timeout-minutes: 45
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5.0.1
        with:
          fetch-depth: 0

      - uses: nixbuild/nix-quick-install-action@2c9db80fb984ceb1bcaa77cdda3fdf8cfba92035 # v34
        with:
          nix_conf: |
            experimental-features = flakes nix-command # https://nix.dev/manual/nix/unstable/command-ref/conf-file.html#conf-experimental-features
            keep-env-derivations = true
            keep-outputs = true
            # Support enhanced caching.
            auto-optimise-store = true
            trusted-users = root ${{ github.actor }}
            # Improve build performance.
            max-jobs = auto
            cores = 0
            # Configure substituters.
            substituters = https://cache.nixos.org/ https://nix-community.cachix.org
            trusted-public-keys = cache.nixos.org-1:6NCHdD59X431o0gWypbMrAURkbJ16ZPMQFGspcDShjY= nix-community.cachix.org-1:mB9FSh9qf2dCimDSUo8Zy7bkq5CX+/rkCWyvRCYg3Fs=

      - uses: nix-community/cache-nix-action@135667ec418502fa5a3598af6fb9eb733888ce6a # v6.1.3
        with:
          # Restore and save a cache using this key.
          # Include git commit to ensure cache validity for exact source.
          primary-key: nix-${{ runner.os }}-${{ github.sha }}-${{ hashFiles('flake.nix', 'flake.lock') }}
          # If there's no cache hit, restore a cache by this prefix.
          # Use more granular prefixes to get better partial cache hits.
          restore-prefixes-first-match: |
            nix-${{ runner.os }}-${{ github.sha }}-${{ hashFiles('flake.nix', 'flake.lock') }}
            nix-${{ runner.os }}-${{ hashFiles('flake.nix', 'flake.lock') }}
            nix-${{ runner.os }}-${{ hashFiles('flake.nix') }}
            nix-${{ runner.os }}-
          # Cache additional paths for pre-commit and other tools.
          paths: |
            ~/.cache/pre-commit/
          # Explicitly save the cache.
          save: true
          # Enable nix store caching. # https://github.com/nix-community/cache-nix-action/blob/e2cf51da82e145785f5db595f553f7cbc2ca54df/action.yml#L63
          nix: true
          # Collect garbage until the Nix store size is at most this number before trying to save a new cache.
          gc-max-store-size-linux: 2G
          # Clear old caches to prevent stale references.
          purge: true
          # Purge ALL cache versions for this workflow to prevent stale store path issues.
          purge-prefixes: |
            nix-${{ runner.os }}-
          # Purge caches older than 12 hours to prevent stale store path references.
          purge-created: 43200 # 43200 seconds is 12 hours.
          # Never purge the current primary key.
          purge-primary-key: never

      - name: Build and Test
        timeout-minutes: 45 # Allow sufficient buffer time for Nix to rebuild Nix store.
        run: |
          echo "=== Debugging Information ==="
          echo "Working directory: $(pwd)"
          echo "Nix version: $(nix --version)"
          echo "Git commit: ${{ github.sha }}"
          echo "Available files:"
          ls -la

          echo "=== Cleaning up potentially stale Nix store references ==="
          # Clean up any stale references that might cause issues
          nix store gc --option keep-going true || echo "Store GC completed with warnings"
          # Repair any corruption that might have occurred
          nix store repair --option keep-going true || echo "Store repair completed with warnings"

          echo "=== Building Development Shell ==="
          # Detect current system architecture
          SYSTEM=$(nix eval --impure --raw --expr 'builtins.currentSystem')
          echo "Detected system: $SYSTEM"

          # Show available flake outputs
          echo "=== Available Flake Outputs ==="
          nix flake show

          # Build the development shell using the detected system architecture
          echo "=== Building Development Shell ==="
          nix build ".#devShells.${SYSTEM}.default" --no-link

          echo "✅ Development shell build completed successfully"

      - name: Verify Development Environment
        timeout-minutes: 1
        run: |
          echo "=== Verify Development Environment ==="
          # Repair Nix store if there are any corruption issues
          nix store repair --option keep-going true || echo "Store repair completed with warnings"

          nix develop --command bash -c "
            echo 'Successfully entered nix develop environment.'
            echo 'PATH: '\$PATH
            echo 'Checking for required tools...'
            if ! command -v pre-commit &> /dev/null; then
              echo '❌ Missing pre-commit tool.'
              echo 'Available commands in environment:'
              compgen -c | grep -E '^(pre-commit|terraform|keep-sorted|az)' | head -10
              exit 1
            fi
            echo '✅ Found pre-commit '\$(pre-commit --version | cut -d' ' -f2)' in Nix environment.'
          "

      - name: Install Pre-Commit
        timeout-minutes: 45 # Nix store should already be built and cached. But, install hooks from external sources may take time.
        run: |
          nix develop --command bash -c "
            echo 'Installing pre-commit hooks...'
            pre-commit install-hooks
          "

      - name: Run Pre-Commit
        timeout-minutes: 45 # Nix store should already be built and cached. But, running hooks on all files may take time.
        run: |
          nix develop --command bash -c "
            echo 'Pre-Commit Version: \$(pre-commit --version)'
            echo 'Installing pre-commit hooks...'
            pre-commit install-hooks

            # Determine which files to check
            if [[ '${{ github.event_name }}' == 'push' && '${{ github.ref }}' == 'refs/heads/main' ]]; then
              echo 'Running pre-commit on all files (push to main branch)'
              pre-commit run --all-files
            elif [[ '${{ github.event_name }}' == 'pull_request' ]]; then
              echo 'Running pre-commit on changed files only'
              pre-commit run --from-ref=origin/${{ github.base_ref }} --to-ref=HEAD
            else
              echo 'Running pre-commit on all files as fallback'
              pre-commit run --all-files
            fi
          "
